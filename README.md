# lim-lang

## Language purpose
_" Simple is best, modern prototyping should be fast, limits should be gone. "_

## To install dependencies:

```bash
bun install
```

To run:

```bash
bun run foo.lim
```
